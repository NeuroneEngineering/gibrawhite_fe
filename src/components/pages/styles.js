import styled from "styled-components";

export const PageWrapper = styled.div`
  box-sizing: border-box;
  padding: 0px 50px;

  background: radial-gradient(50% 50% at 50% 50%, #ffffff 0%, #fcf7ff 100%);
`;
